import React, { FC } from 'react'
import styled from 'styled-components/macro'

const GLViewContainer = styled.div`

`


const GLView: FC = () => {

  return (
    <GLViewContainer>
      1
    </GLViewContainer>
  );
};

export default GLView;